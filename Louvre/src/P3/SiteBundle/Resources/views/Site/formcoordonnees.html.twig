<div class="well">
    {{ form_start(form) }}
    
    {# Les erreurs générales du formulaire. #}
  <div id="erreur">{{ form_errors(form) }}</div>
{% for individu in form.individus%}
    <div class="form-group">
  {# Génération du label. #}
  {{ form_label(individu,"Billet n°", {'label_attr':{'class':'control-label'}}) }}{{loop.index}}
    <div id="erreur">{{ form_errors(individu) }}</div>
    </div>    
    <div class="form-group">
    {# Génération du label. #}
    {{ form_label(individu.nom,"Nom", {'label_attr':{'class':'control-label'}}) }}
    
    {# Génération de l'input. #}
        {{ form_widget(individu.nom, {'attr': {'class': 'form-control'}}) }}
    {# Affichage des erreurs pour ce champ précis. #}
    <div id="erreur">{{ form_errors(individu.nom) }}</div>
        
    </div>
    <div class="form-group">
    {# Génération du label. #}
    {{ form_label(individu.prenom,"Prénom", {'label_attr':{'class':'control-label'}}) }}
    
    {# Génération de l'input. #}
        {{ form_widget(individu.prenom, {'attr': {'class': 'form-control'}}) }}
    {# Affichage des erreurs pour ce champ précis. #}
    <div id="erreur">{{ form_errors(individu.prenom) }}</div>
        
    </div>
    <div class="form-group">
    {# Génération du label. #}
    {{ form_label(individu.pays,"Pays", {'label_attr':{'class':'control-label'}}) }}
    {# Génération de l'input. #}
        {{ form_widget(individu.pays, {'attr': {'class': 'form-control'}}) }}
    {# Affichage des erreurs pour ce champ précis. #}
    <div id="erreur">{{ form_errors(individu.pays) }}</div>
    </div>
    <div class="form-group">
  {# Génération du label. #}
  {{ form_label(individu.datedenaissance, "Date de Naissance", {'label_attr':{'class':'control-label'}}) }}
    
    <div class="form-inline">
        
        {# Génération de l'input. #}
        {{ form_widget(individu.datedenaissance.day, {'attr': {'class': 'form-control'}}) }}
        
        {# Génération de l'input. #}
        {{ form_widget(individu.datedenaissance.month, {'attr': {'class': 'form-control'}}) }}
        
        {# Génération de l'input. #}
        {{ form_widget(individu.datedenaissance.year, {'attr': {'class': 'form-control'}}) }}
        
    </div>
        {# Affichage des erreurs pour ce champ précis. #}
    <div id="erreur">{{ form_errors(individu.datedenaissance) }}</div>
    
    </div>
    <div class="form-group">
    
    
    {# Génération du label. #}
    {{ form_label(individu.tarifreduit, "Tarif réduit ? ", {'label_attr':{'class':'control-label'}}) }}
    
    
    {# Génération de l'input. #}
    {{ form_widget(individu.tarifreduit)}}
    
    {# Affichage des erreurs pour ce champ précis. #}
    <div id="erreur">{{ form_errors(individu.tarifreduit) }}</div>
   
    </div>
{% endfor %}
    <div class="form-group">
    {# Génération du label. #}
    {{ form_label(form.email, "Adresse Email ", {'label_attr':{'class':'control-label'}}) }}
           
    {# Génération de l'input. #}
    {{ form_widget(form.email, {'attr': {'class': 'form-control'}})}}
        
    {# Affichage des erreurs pour ce champ précis. #}
    <div id="erreur">{{ form_errors(form.email) }}</div>
        
        
    
    </div>
        <div class="form-group">
        {{ form_widget(form.Suivant, {'attr': {'class': 'btn btn-primary'}}) }}
 
        </div>
  {# Génération automatique des champs pas encore écrits.#}
     
  {{ form_rest(form) }}

  {# Fermeture de la balise <form> du formulaire HTML #}
  {{ form_end(form) }}
<em>*Si vous bénéficiez d'un tarif réduit, un justificatif (carte étudiant, carte militaire ou autre) vous sera demandé à l'entrée .</em>        
</div>